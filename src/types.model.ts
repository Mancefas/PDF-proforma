export type numberAndDateTypes = {
  proformaNumber: string;
  proformaDate: string;
};

export type buyerTypes = {
  buyerCompanyName: string;
  buyerAddress: string;
  buyerCompanyCode: string;
};

export type sellerTypes = {
  sellerCompanyName: string;
  sellerAddress: string;
  sellerCompanyCode: string;
  sellerBankAcc: string;
};

export type goodsTypes = {
  goodsName: string;
  unit: string;
  quantity: number;
  price: number;
};
